import { createReducer } from "@reduxjs/toolkit";

const foldersReducer = createReducer([], (builder) => {
    builder
        .addCase("CREATE_FOLDER", (state, action) => {

        })
        .addCase("READ_FOLDER", (state, action) => {

        })
        .addCase("UPDATE_FOLDER", (state, action) => {
            
        })
        .addCase("DELETE_FOLDER", (state, action) => {

        })
        .addCase("SET_UP_FOLDERS", (state, action) => {
        
        })
        
});